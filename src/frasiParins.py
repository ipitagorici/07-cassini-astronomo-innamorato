from manim import *

class Frase1(Scene):
    def construct(self):
        frase = Tex("skibidi")

        frase.move_to(ORIGIN)
        self.play(Write(frase))
        self.wait(2)
        
class Frase2(Scene):
    def construct(self):
        frase = Tex("skibidi")

        frase.move_to(ORIGIN)
        self.play(Write(frase))
        self.wait(2)

class Frase3(Scene):
    def construct(self):
        frase = Tex("skibidi")

        frase.move_to(ORIGIN)
        self.play(Write(frase))
        self.wait(2)

class Frase4(Scene):
    def construct(self):
        frase = Tex("skibidi")

        frase.move_to(ORIGIN)
        self.play(Write(frase))
        self.wait(2)

class Frase5(Scene):
    def construct(self):
        frase = Tex("skibidi")

        frase.move_to(ORIGIN)
        self.play(Write(frase))
        self.wait(2)

class Frase6(Scene):
    def construct(self):
        frase = Tex("skibidi")

        frase.move_to(ORIGIN)
        self.play(Write(frase))
        self.wait(2)

class Frase7(Scene):
    def construct(self):
        frase = Tex("skibidi")

        frase.move_to(ORIGIN)
        self.play(Write(frase))
        self.wait(2)

class Frase8(Scene):
    def construct(self):
        frase = Tex("skibidi")

        frase.move_to(ORIGIN)
        self.play(Write(frase))
        self.wait(2)

class Frase9(Scene):
    def construct(self):
        frase = Tex("skibidi")

        frase.move_to(ORIGIN)
        self.play(Write(frase))
        self.wait(2)

class Frase10(Scene):
    def construct(self):
        frase = Tex("skibidi")

        frase.move_to(ORIGIN)
        self.play(Write(frase))
        self.wait(2)

class Frase11(Scene):
    def construct(self):
        frase = Tex("skibidi")

        frase.move_to(ORIGIN)
        self.play(Write(frase))
        self.wait(2)

class Frase12(Scene):
    def construct(self):
        frase = Tex("skibidi")

        frase.move_to(ORIGIN)
        self.play(Write(frase))
        self.wait(2)